{% extends 'base.html.twig' %}

{% block stylesheets %}
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.1/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-+0n0xVW2eSR5OomGNYDnhzAbDsOXxcvSN1TPprVMTNDbiYZCxYbOOl7+AMvyTG2x" crossorigin="anonymous">
{% endblock %}


{% block body %}

    {% for type, messages in app.flashes %}
        {% for un_message in messages %}
            <p class="alert alert-{{type}}">{{ un_message }}</p>
        {% endfor %}
    {% endfor %}

    <h1>Série - Admin - Editer</h1>
    <hr>

    {% if categorie is defined %}
        {% if categorie is empty %}
            <p>Aucune catégorie trouvée</p>
        {% else %}
            <h2>{{ categorie.cat }}</h2>

        {% endif %}
    {% else %}
        {% if genre is defined %}
            {% if genre is empty %}
                <p>Aucun genre trouvé</p>
            {% else %}
                <h2>{{ genre.gen }}</h2>

            {% endif %}
        {% else %}
            {% if serie is defined %}
                {% if serie is empty %}
                    <p>Aucune série trouvée</p>
                {% else %}
                    <p>Nom : <b>{{ serie.nom }}</b></p>
                    <p>Mots associés : <b>{{ serie.keywords }}</b></p>
                    <p>Résumé : <b>{{ serie.resume }}</b></p>
                    <p>Catégorie liée : <b>{{ serie.categorie.cat }}</b></p>
                    <p>Genre lié : <b>{{ serie.genre.gen }}</b></p>
                {% endif %}
            {% endif %}
        {% endif %}
    {% endif %}

    {{ form(edit) }}

{% endblock %}
